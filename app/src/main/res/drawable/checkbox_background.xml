<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- When checked -->
    <item android:drawable="@color/checked_color" android:state_checked="true"/>
    <!-- When not checked -->
    <item android:drawable="@color/unchecked_color"/>
</selector>
